# Cycle 82 review — export controls

After Fomawrite Dev is refreshed, open Export and scroll both the left options and middle styles columns. The scrollbar thumbs should remain inside their columns, away from the “Built-in styles” heading and card text. Check that style cards, Open paginated preview, Share Markdown and Cancel look clickable before hover, then gain a stronger outline on hover. In the live preview, Split should show read-only Markdown alongside rendered output; Full should return to the rendered output alone. Repeat at a narrow window size and in dark mode.
